import {slides} from "./slides.js";import {stateRef,set,onValue,db,ref} from "./firebase.js";
const list=document.querySelector("#slides"),status=document.querySelector("#status");let current={slide:"esteban",inverted:false,chroma:"R"};
for(const s of slides){const row=document.createElement("article");row.className="slide-row";row.dataset.id=s.id;row.innerHTML='<div><small>'+s.section+'</small><strong></strong><em>'+s.type+'</em></div><button type="button">ARM</button>';row.querySelector("strong").textContent=s.label;row.querySelector("button").onclick=async()=>{current={...current,slide:s.id};if(s.type!=="perception")current.inverted=false;await set(stateRef,current)};if(s.type==="chroma"){const rgb=document.createElement("div");rgb.className="rgb";for(const c of ["R","G","B"]){const b=document.createElement("button");b.textContent=c;b.onclick=async()=>{current={...current,slide:s.id,chroma:c,inverted:false};await set(stateRef,current)};rgb.append(b)}row.append(rgb)}list.append(row)}
onValue(stateRef,s=>{current={...current,...(s.val()||{})};status.textContent="LIVE · "+current.slide;document.querySelectorAll(".slide-row").forEach(x=>x.classList.toggle("live",x.dataset.id===current.slide))});
document.querySelector("#reset").onclick=()=>set(ref(db,"presentation"),{state:{slide:"esteban",inverted:false,chroma:"R"},entryCounter:0});
